#include "librarySystem.h"
#include <iostream>

int main()
{
    LibrarySystem library;
    library.start();

    /*std::string str = "one two three";
    std::vector<std::string> vec = library.divideCommand(str);

    for (size_t i = 0; i < vec.size(); i++)
    {
        std::cout << vec[i] << '\n';
    }*/
}
